{
  "name": "hw",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "parcel ./src/pages/*.html",
    "build": "parcel build ./src/pages/*.html --no-source-maps --public-url ./",
    "deploy:pages": "rimraf dist && npm run build && gh-pages -d dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^6.2.0",
    "parcel": "^2.12.0",
    "rimraf": "^6.0.1",
    "svgo": "^3.3.2"
  }
}
